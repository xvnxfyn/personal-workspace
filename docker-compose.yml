version: "3.9"

services:
  personal-workspace:
    build: .
    container_name: personal-workspace-app
    ports:
      - "80:3000"
    restart: unless-stopped
    volumes:
      - prisma_data:/app/prisma
    environment:
      - NODE_ENV=production

volumes:
  prisma_data: